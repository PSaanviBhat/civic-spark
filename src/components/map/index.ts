/**
 * Map Components
 * Components for map functionality
 */

// Example: import { MapContainer } from './MapContainer';
// export { MapContainer };

// NOTE: Create map-specific components here:
// - MapContainer.tsx (main map wrapper)
// - IssueMarker.tsx (custom marker)
// - MapControls.tsx (zoom, filters, etc.)
